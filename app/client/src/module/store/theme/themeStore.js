const DEFAULT_STATE = {
  color1: '603a9a',
  color2: '9C92AC'
};

export const themeReducer = (state = DEFAULT_STATE, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};
